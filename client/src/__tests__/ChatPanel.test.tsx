import { describe, it } from "vitest";

// Placeholder suite to keep file active without failing CI until real tests are added.
// Skipped to avoid impacting coverage and runtime.
describe.skip("ChatPanel", () => {
  it("placeholder", () => {
    // Intentionally empty
  });
});
