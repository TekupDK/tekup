# Agents Hub environment configuration

# Base API for flow-api (metrics and agent control)
NEXT_PUBLIC_API_URL=http://localhost:4000

# Optional API key if flow-api enforces API key auth
NEXT_PUBLIC_TENANT_API_KEY=dev-key
