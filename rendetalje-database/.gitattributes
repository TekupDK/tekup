# Git-crypt configuration for encrypted files
# Uncomment and configure if using git-crypt for sensitive data

# Auto detect text files and normalize line endings to LF
* text=auto

# SQL files
*.sql text eol=lf

# Shell scripts
*.sh text eol=lf
*.ps1 text eol=crlf

# Documentation
*.md text eol=lf

# JSON
*.json text eol=lf

# YAML
*.yml text eol=lf
*.yaml text eol=lf

# Environment files (if using git-crypt)
# .env.* filter=git-crypt diff=git-crypt
# .env filter=git-crypt diff=git-crypt